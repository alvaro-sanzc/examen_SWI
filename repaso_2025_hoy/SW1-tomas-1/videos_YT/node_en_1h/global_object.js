// GLOBAL
// console.log();
// setTimeout();   // call something after a delay
// clearTimeout();
// setInterval();  // call something repeatedly after a delay
// clearInterval();

// window, global -> variables globales que podemos usar para llamar a todas estas funciones (no es necesario)

var message = ''    // -> tamben puede ser identificado con la variable window
console.log(global.message) // -> undefined